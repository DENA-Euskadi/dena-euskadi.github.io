# Diagramas (draw.io)

Diagramas de arquitectura en formato `draw.io` y su exportación a imagen.

> Ubicar los `.drawio` y sus exportaciones (`.png` / `.svg`) en `docs/adjuntos/imagenes/arquitectura/`.
